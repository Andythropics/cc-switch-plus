# Separate Skill Acquisition from Deployment

Installing or importing a Skill will place it only in the Skill Library and will never implicitly create a Global or Project Deployment. After acquisition, users may explicitly choose one or more deployment targets from either the Library or Workspace views. This replaces the current install-and-enable behavior so reviewing or centrally curating a Skill cannot transiently expose it to a consumer.
